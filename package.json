{
  "scripts": {
    "build": "npm run build --prefix frontend",
    "sync": "npx cap sync",
    "build:app": "npm run build && npx cap sync",
    "generate:icons": "bash fix-foreground.sh"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "pillow": "^0.0.9"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
