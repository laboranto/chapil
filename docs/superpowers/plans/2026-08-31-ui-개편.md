# 차필 UI 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 차필 프런트엔드의 내비게이션·홈·기록 리스트·입력 양식을 재구성하고, 아이콘을 Noto Emoji 추출본으로 교체하며 재생성 파이프라인을 repo에 둔다.

**Architecture:** 3계층으로 나눠 진행한다. (1) 아이콘 파이프라인 — `fonttools`로 글리프를 SVG로 뽑는 Python 스크립트 + vendoring된 폰트. (2) 데이터 계층 — `pagination.js`에 UNION 키셋 쿼리를 추가하고 `api.js`에 통합 기록 조회 함수를 만든다. (3) UI — 공유 컴포넌트(SegmentTabs, 기록 카드 3종)를 먼저 만들고 페이지(Records, RecordForm, Home)와 라우팅을 재작성한다.

**Tech Stack:** React 18, react-router-dom, Vite, vite-plugin-svgr, @sqlite.org/sqlite-wasm(로컬 DB), vitest(순수 로직 테스트만), Python + fonttools(빌드 외 스크립트).

## Global Constraints

- 응답·주석·커밋 메시지는 한국어. 커밋 메시지 말미에 `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` 와 `Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK`.
- 이 프로젝트는 컴포넌트 테스트 도구(RTL)가 없다. **순수 로직(pagination, api 쿼리)만 vitest로 TDD**하고, React 컴포넌트 task는 `npm run lint` + `npm run build` + 각 task 말미의 수동 체크리스트로 검증한다. RTL 등 새 테스트 의존성 추가 금지.
- 새 런타임 의존성 추가 금지. (fonttools는 dev 스크립트용 Python 패키지로 repo의 npm 의존성 아님.)
- 추출 SVG는 `fill="currentColor"` 단일 `<path>`, bbox 정사각 viewBox(+8% 패딩), `transform="scale(1 -1)"` y축 뒤집기.
- `src` 정렬 우선순위 고정: `fuel` < `maintenance` < `other` (SQL 리터럴 문자열 비교).
- 통합 리스트 정렬: `ORDER BY date DESC, src ASC, id DESC`.
- 작업 디렉터리는 `frontend/`. 명령·경로는 별도 명시 없으면 `frontend/` 기준.
- 커밋은 각 task의 마지막 스텝에서. task 중간 스텝에서 커밋하지 않는다.

스펙 원문: `docs/superpowers/specs/2026-08-31-ui-개편-design.md`

---

## File Structure

**생성**
- `frontend/scripts/extract-emoji.py` — 글리프 → SVG 추출
- `frontend/scripts/NotoEmoji-Regular.ttf` — vendoring 폰트 (~2MB)
- `frontend/scripts/NotoEmoji-OFL.txt` — 라이선스
- `frontend/src/assets/symbols/kebab.svg` — 손으로 작성(파이프라인 예외)
- `frontend/src/assets/symbols/generated.test.js` — 추출 결과 검증
- `frontend/src/pagination.union.test.js` — UNION 커서 단위 테스트
- `frontend/src/records-pagination.integration.test.js` — 3테이블 페이지네이션 통합 테스트
- `frontend/src/components/SegmentTabs.jsx` — 세그먼트 탭(필터·양식 공유)
- `frontend/src/components/records/FuelCard.jsx`
- `frontend/src/components/records/MaintenanceCard.jsx`
- `frontend/src/components/records/OtherCard.jsx`
- `frontend/src/components/records/recordType.js` — `src` → 아이콘/라벨 매핑
- `frontend/src/pages/Records.jsx` — 통합 기록 리스트 + 필터 탭
- `frontend/src/pages/RecordForm.jsx` — 신규 입력 세그먼트 탭 래퍼

**수정**
- `frontend/package.json` — `icons` 스크립트
- `frontend/src/pagination.js` — `buildKeysetUnionQuery`, `nextUnionCursorFrom`
- `frontend/src/api.js` — `getRecordsPage`, `getRecentRecords`, `getDashboard`; `getFuelPage`/`getMaintenancePage`/`getOtherPage` 제거
- `frontend/src/pages/FuelForm.jsx` / `MaintenanceForm.jsx` / `OtherForm.jsx` — `forwardRef` + `mode` prop, topbar 조건부
- `frontend/src/pages/Home.jsx` — 정체성 블록 + 요약 타일 + 통합 최근기록, topbar/푸터/gear 제거
- `frontend/src/pages/Settings.jsx` — 버전 표기, import/export 아이콘
- `frontend/src/components/BottomNav.jsx` — 3링크 + 새 아이콘
- `frontend/src/components/MoreMenu.jsx` — `other.svg` → `kebab.svg`
- `frontend/src/App.jsx` — 라우팅 재편
- `frontend/src/index.css` — `.segment-tabs`, `.summary-tile`, `.identity-block`, `.content.no-topbar`; `.car-profile h1`·`.topbar-home` 제거; `.bottom-nav` 폭

**삭제** (Task 10에서 일괄)
- `frontend/src/pages/FuelList.jsx` / `MaintenanceList.jsx` / `OtherList.jsx`
- `frontend/src/assets/symbols/oil_and_electric.svg` / `gear.svg` / `download.svg` / `upload.svg`
- `frontend/src/assets/defaults/profile.svg`

---

## Task 1: 아이콘 추출 파이프라인 + 전체 SVG 생성

**Files:**
- Create: `frontend/scripts/extract-emoji.py`, `frontend/scripts/NotoEmoji-Regular.ttf`, `frontend/scripts/NotoEmoji-OFL.txt`, `frontend/src/assets/symbols/kebab.svg`, `frontend/src/assets/symbols/generated.test.js`
- Modify: `frontend/package.json`
- Generated (커밋 대상): `frontend/src/assets/symbols/{home,records,settings,fuel,charge,maintenance,other,car,import,export,copy}.svg`

**Interfaces:**
- Produces: `src/assets/symbols/<name>.svg` 11종 + `kebab.svg`. 각 파일은 `<svg viewBox="…"><path … fill="currentColor"/></svg>` 형태. 이후 모든 UI task가 `import X from '../assets/symbols/<name>.svg?react'`로 사용.

- [ ] **Step 1: 폰트·라이선스 vendoring**

```bash
mkdir -p frontend/scripts
cp "/home/iranto/Cloud/소프트웨어/폰트/Noto_Emoji/static/NotoEmoji-Regular.ttf" frontend/scripts/NotoEmoji-Regular.ttf
cp "/home/iranto/Cloud/소프트웨어/폰트/Noto_Emoji/OFL.txt" frontend/scripts/NotoEmoji-OFL.txt
```

- [ ] **Step 2: 추출 스크립트 작성**

`frontend/scripts/extract-emoji.py`:

```python
#!/usr/bin/env python3
"""NotoEmoji-Regular.ttf에서 지정 글리프만 뽑아 currentColor SVG로 저장.
사용: python scripts/extract-emoji.py src/assets/symbols"""
import sys
from pathlib import Path
from fontTools.ttLib import TTFont
from fontTools.pens.svgPathPen import SVGPathPen
from fontTools.pens.boundsPen import BoundsPen

FONT = Path(__file__).parent / "NotoEmoji-Regular.ttf"
OUT = Path(sys.argv[1]) if len(sys.argv) > 1 else Path("src/assets/symbols")
PAD = 0.08  # 정사각 변 대비 여백 비율

# 이름: 유니코드 코드포인트. 아이콘 추가는 여기 한 줄.
TARGETS = {
    "home":        0x1F3E0,  # 🏠
    "records":     0x1F9FE,  # 🧾
    "settings":    0x2699,   # ⚙️
    "fuel":        0x26FD,   # ⛽
    "charge":      0x1F50C,  # 🔌
    "maintenance": 0x1F527,  # 🔧
    "other":       0x1F4E6,  # 📦
    "car":         0x1F697,  # 🚗
    "import":      0x1F4E5,  # 📥
    "export":      0x1F4E4,  # 📤
    "copy":        0x1F4CB,  # 📋
}

def main():
    OUT.mkdir(parents=True, exist_ok=True)
    font = TTFont(FONT)
    cmap = font.getBestCmap()
    gs = font.getGlyphSet()
    for name, cp in TARGETS.items():
        gname = cmap[cp]
        bp = BoundsPen(gs)
        gs[gname].draw(bp)
        xMin, yMin, xMax, yMax = bp.bounds
        sp = SVGPathPen(gs)
        gs[gname].draw(sp)
        d = sp.getCommands()
        cx, cy = (xMin + xMax) / 2, (yMin + yMax) / 2
        box = max(xMax - xMin, yMax - yMin) * (1 + 2 * PAD)
        vx, vy = cx - box / 2, -cy - box / 2  # scale(1 -1) 후 중심은 (cx, -cy)
        svg = (
            f'<svg xmlns="http://www.w3.org/2000/svg" '
            f'viewBox="{vx:.1f} {vy:.1f} {box:.1f} {box:.1f}">'
            f'<path transform="scale(1 -1)" d="{d}" fill="currentColor"/></svg>\n'
        )
        (OUT / f"{name}.svg").write_text(svg)
        print(f"{name}: {gname}")

if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 케밥 아이콘 수동 작성**

`frontend/src/assets/symbols/kebab.svg`:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><circle cx="12" cy="5" r="2" fill="currentColor"/><circle cx="12" cy="12" r="2" fill="currentColor"/><circle cx="12" cy="19" r="2" fill="currentColor"/></svg>
```

- [ ] **Step 4: package.json 스크립트 추가**

`frontend/package.json`의 `scripts`에 추가:

```json
"icons": "python scripts/extract-emoji.py src/assets/symbols"
```

- [ ] **Step 5: 추출 실행**

Run: `cd frontend && npm run icons`
Expected: `home: … / records: … / …` 11줄 출력, `src/assets/symbols/`에 11개 `.svg` 갱신·생성. (기존 `home.svg`, `maintenance.svg`, `other.svg`, `copy.svg`는 덮어써짐. `oil_and_electric.svg` 등은 그대로 남음 — Task 10에서 삭제.)

- [ ] **Step 6: 검증 테스트 작성**

`frontend/src/assets/symbols/generated.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import { fileURLToPath } from 'node:url'

const NAMES = ['home','records','settings','fuel','charge','maintenance','other','car','import','export','copy','kebab']
const dir = fileURLToPath(new URL('.', import.meta.url))

describe('생성된 아이콘 SVG', () => {
  for (const name of NAMES) {
    it(`${name}.svg — currentColor 단일 path + 유효한 viewBox`, () => {
      const svg = readFileSync(`${dir}${name}.svg`, 'utf-8')
      expect(svg).toContain('fill="currentColor"')
      expect(svg.match(/<path/g)).toHaveLength(1)
      const vb = svg.match(/viewBox="([^"]+)"/)[1].trim().split(/\s+/).map(Number)
      expect(vb).toHaveLength(4)
      expect(vb.every(Number.isFinite)).toBe(true)
      expect(vb[2]).toBeGreaterThan(0)
      expect(vb[3]).toBeGreaterThan(0)
    })
  }
})
```

- [ ] **Step 7: 테스트 실행**

Run: `cd frontend && npx vitest run src/assets/symbols/generated.test.js`
Expected: 12 passed

- [ ] **Step 8: 커밋**

```bash
git add frontend/scripts frontend/package.json frontend/src/assets/symbols
git commit -m "feat: Noto Emoji 아이콘 추출 파이프라인 + 아이콘 세트 생성

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 2: pagination.js — UNION 키셋 쿼리

**Files:**
- Modify: `frontend/src/pagination.js`
- Test: `frontend/src/pagination.union.test.js`

**Interfaces:**
- Consumes: 기존 `PAGE_SIZE`, `buildKeysetQuery`, `nextCursorFrom` (변경 없음, 그대로 둔다).
- Produces:
  - `buildKeysetUnionQuery(cursor, limit) => { sql, params }` — `cursor`는 `null` 또는 `{ date, src, id }`.
  - `nextUnionCursorFrom(rows, limit) => { date, src, id } | null`.

- [ ] **Step 1: 실패하는 테스트 작성**

`frontend/src/pagination.union.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { buildKeysetUnionQuery, nextUnionCursorFrom } from './pagination'

describe('buildKeysetUnionQuery', () => {
  it('cursor가 null이면 WHERE 없이 UNION ALL 3개 + 정렬', () => {
    const { sql, params } = buildKeysetUnionQuery(null, 50)
    expect(sql.match(/UNION ALL/g)).toHaveLength(2)
    expect(sql).not.toContain('WHERE')
    expect(sql).toContain('ORDER BY date DESC, src ASC, id DESC LIMIT ?')
    expect(params).toEqual([50])
  })

  it('cursor가 있으면 3-OR WHERE + params 순서', () => {
    const { sql, params } = buildKeysetUnionQuery({ date: '2026-03-10', src: 'maintenance', id: 7 }, 50)
    expect(sql).toContain(
      'WHERE date < ? OR (date = ? AND src > ?) OR (date = ? AND src = ? AND id < ?)'
    )
    expect(params).toEqual(['2026-03-10', '2026-03-10', 'maintenance', '2026-03-10', 'maintenance', 7, 50])
  })

  it('fuel/maintenance/other 리터럴 src를 SELECT한다', () => {
    const { sql } = buildKeysetUnionQuery(null, 10)
    expect(sql).toContain("'fuel' AS src")
    expect(sql).toContain("'maintenance' AS src")
    expect(sql).toContain("'other' AS src")
  })
})

describe('nextUnionCursorFrom', () => {
  it('빈 배열이면 null', () => {
    expect(nextUnionCursorFrom([], 50)).toBeNull()
  })
  it('limit보다 적으면 null', () => {
    expect(nextUnionCursorFrom([{ date: '2026-01-01', src: 'fuel', id: 1 }], 50)).toBeNull()
  })
  it('정확히 limit개면 마지막 행의 {date,src,id}', () => {
    const rows = [
      { date: '2026-01-02', src: 'fuel', id: 2 },
      { date: '2026-01-01', src: 'other', id: 5 },
    ]
    expect(nextUnionCursorFrom(rows, 2)).toEqual({ date: '2026-01-01', src: 'other', id: 5 })
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/pagination.union.test.js`
Expected: FAIL — `buildKeysetUnionQuery is not a function`

- [ ] **Step 3: 구현**

`frontend/src/pagination.js` 끝에 추가:

```js
// ── 통합 기록(3테이블 UNION) 키셋 페이지네이션 ──────────────────────
// src 우선순위: fuel < maintenance < other (리터럴 문자열 비교).
// 정렬: date DESC, src ASC, id DESC.

const UNION_BODY =
  "SELECT id, date, 'fuel' AS src, amount, odometer, interval_km, liters, unit_price, fuel_economy, NULL AS item, memo FROM fuel" +
  " UNION ALL " +
  "SELECT id, date, 'maintenance' AS src, amount, odometer, NULL, NULL, NULL, NULL, item, memo FROM maintenance" +
  " UNION ALL " +
  "SELECT id, date, 'other' AS src, amount, odometer, NULL, NULL, NULL, NULL, item, memo FROM other"

const UNION_ORDER = 'ORDER BY date DESC, src ASC, id DESC LIMIT ?'

export function buildKeysetUnionQuery(cursor, limit) {
  if (!cursor) {
    return { sql: `SELECT * FROM (${UNION_BODY}) ${UNION_ORDER}`, params: [limit] }
  }
  return {
    sql:
      `SELECT * FROM (${UNION_BODY}) ` +
      'WHERE date < ? OR (date = ? AND src > ?) OR (date = ? AND src = ? AND id < ?) ' +
      UNION_ORDER,
    params: [cursor.date, cursor.date, cursor.src, cursor.date, cursor.src, cursor.id, limit],
  }
}

export function nextUnionCursorFrom(rows, limit) {
  if (rows.length < limit) return null
  const last = rows[rows.length - 1]
  return { date: last.date, src: last.src, id: last.id }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && npx vitest run src/pagination.union.test.js`
Expected: PASS (9 passed)

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/pagination.js frontend/src/pagination.union.test.js
git commit -m "feat: 통합 기록 UNION 키셋 페이지네이션 쿼리

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 3: api.js — 통합 기록 조회

**Files:**
- Modify: `frontend/src/api.js`
- Test: `frontend/src/records-pagination.integration.test.js`

**Interfaces:**
- Consumes: Task 2의 `buildKeysetUnionQuery`, `nextUnionCursorFrom`; 기존 `buildKeysetQuery`, `nextCursorFrom`, `PAGE_SIZE`; `getDB()`.
- Produces:
  - `api.getRecordsPage({ cursor = null, filter = null, limit = PAGE_SIZE }) => { rows, nextCursor }`
    - `filter ∈ { null, 'fuel', 'maintenance', 'other' }`.
    - `filter` 있으면 단일 테이블(`buildKeysetQuery(filter, cursor, limit)`), 커서 `{ date, id }`. 반환 행에 `src: filter` 를 채워 넣는다.
    - `filter` 없으면 `buildKeysetUnionQuery`, 커서 `{ date, src, id }`.
  - `api.getRecentRecords() => rows[]` — UNION, 커서 없음, `LIMIT 10`, 정렬 동일. 각 행에 `src` 포함.
  - `api.getDashboard()` — 기존 반환에 `recent: rows[]`(= `getRecentRecords()` 결과) 추가. 기존 `recent_fuel`/`last_maintenance`/`last_other` 키는 **이 task에서는 유지**(Task 9에서 제거).
- 제거: `api.getFuelPage`, `api.getMaintenancePage`, `api.getOtherPage`.

- [ ] **Step 1: 실패하는 통합 테스트 작성**

`frontend/src/records-pagination.integration.test.js` — 기존 `pagination.integration.test.js`의 sqlite-wasm 로더 패턴을 그대로 따른다:

```js
/**
 * Integration test: 통합 기록 UNION 페이지네이션.
 * 3개 테이블에 날짜가 겹치는 행을 심고 limit=2로 끝까지 페이지네이션 →
 * 중복·누락 없이 date DESC, src ASC, id DESC 순서로 나오는지 검증.
 * sqlite-wasm 로더는 pagination.integration.test.js와 동일하게 node 전용 엔트리 직접 import.
 */
import { describe, it, expect, beforeAll } from 'vitest'
import { buildKeysetUnionQuery, nextUnionCursorFrom } from './pagination.js'
import sqlite3NodeInit from '../node_modules/@sqlite.org/sqlite-wasm/sqlite-wasm/jswasm/sqlite3-node.mjs'

const SCHEMA = `
  CREATE TABLE fuel (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT, amount INTEGER, odometer INTEGER, interval_km INTEGER, liters REAL, unit_price INTEGER, fuel_economy REAL, memo TEXT);
  CREATE TABLE maintenance (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT, amount INTEGER, odometer INTEGER, item TEXT, memo TEXT);
  CREATE TABLE other (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT, amount INTEGER, odometer INTEGER, item TEXT, memo TEXT);
`

let db

beforeAll(async () => {
  const sqlite3 = await sqlite3NodeInit()
  db = new sqlite3.oo1.DB(':memory:')
  db.exec(SCHEMA)
  // 같은 날짜 '2026-03-10'에 fuel·maintenance·other 각 1건씩 (src tie-break 유발)
  db.exec("INSERT INTO fuel (date,amount,odometer) VALUES ('2026-03-01',50000,1000),('2026-03-10',60000,1200),('2026-03-20',70000,1500)")
  db.exec("INSERT INTO maintenance (date,amount,odometer,item) VALUES ('2026-03-10',80000,1210,'엔진오일'),('2026-03-05',30000,1100,'점검')")
  db.exec("INSERT INTO other (date,amount,odometer,item) VALUES ('2026-03-10',15000,1220,'세차'),('2026-03-25',9000,1600,'주차')")
})

function page(cursor) {
  const { sql, params } = buildKeysetUnionQuery(cursor, 2)
  const rows = db.exec({ sql, bind: params, rowMode: 'object', returnValue: 'resultRows' })
  return { rows, nextCursor: nextUnionCursorFrom(rows, 2) }
}

describe('UNION 페이지네이션', () => {
  it('중복·누락 없이 전량을 정렬 순서대로 순회한다', () => {
    const all = []
    let cursor = null
    for (let i = 0; i < 20; i++) {
      const { rows, nextCursor } = page(cursor)
      all.push(...rows)
      if (!nextCursor) break
      cursor = nextCursor
    }
    expect(all).toHaveLength(7) // 3 + 2 + 2
    const keys = all.map(r => `${r.src}:${r.id}`)
    expect(new Set(keys).size).toBe(7) // 중복 없음
    // 정렬 단조성: date DESC, 그다음 src ASC, 그다음 id DESC
    const rank = { fuel: 0, maintenance: 1, other: 2 }
    for (let i = 1; i < all.length; i++) {
      const a = all[i - 1], b = all[i]
      const ok =
        a.date > b.date ||
        (a.date === b.date && rank[a.src] < rank[b.src]) ||
        (a.date === b.date && a.src === b.src && a.id > b.id)
      expect(ok, `${a.date}/${a.src}/${a.id} → ${b.date}/${b.src}/${b.id}`).toBe(true)
    }
    // 같은 날짜 2026-03-10 블록은 fuel → maintenance → other 순
    const mar10 = all.filter(r => r.date === '2026-03-10').map(r => r.src)
    expect(mar10).toEqual(['fuel', 'maintenance', 'other'])
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/records-pagination.integration.test.js`
Expected: FAIL (import 또는 단정 실패)

- [ ] **Step 3: pagination 함수만으로 테스트 통과 확인**

이 테스트는 `api.js`가 아니라 `pagination.js` 함수를 직접 쓴다. Task 2 구현이 맞으면 이 시점에서 PASS 해야 한다.

Run: `cd frontend && npx vitest run src/records-pagination.integration.test.js`
Expected: PASS (1 passed). 실패하면 `buildKeysetUnionQuery`의 WHERE 조건식을 재검토.

- [ ] **Step 4: api.js 구현 — import 추가**

`frontend/src/api.js` 상단 import 수정:

```js
import { buildKeysetQuery, buildKeysetUnionQuery, nextCursorFrom, nextUnionCursorFrom, PAGE_SIZE } from './pagination';
```

- [ ] **Step 5: api.js 구현 — 통합 조회 함수**

`api.getFuelPage`/`getMaintenancePage`/`getOtherPage` 3개를 삭제하고 그 자리에:

```js
  // ── 통합 기록 목록 (필터 탭 MVP) ──────────────────────────────────
  getRecordsPage: async ({ cursor = null, filter = null, limit = PAGE_SIZE } = {}) => {
    const db = getDB()
    if (filter) {
      const { sql, params } = buildKeysetQuery(filter, cursor, limit)
      const rs = rows(await db.query(sql, params)).map(r => ({ ...r, src: filter }))
      return { rows: rs, nextCursor: nextCursorFrom(rs, limit) }
    }
    const { sql, params } = buildKeysetUnionQuery(cursor, limit)
    const rs = rows(await db.query(sql, params))
    return { rows: rs, nextCursor: nextUnionCursorFrom(rs, limit) }
  },

  getRecentRecords: async () => {
    const db = getDB()
    const { sql, params } = buildKeysetUnionQuery(null, 10)
    return rows(await db.query(sql, params))
  },
```

- [ ] **Step 6: api.js 구현 — getDashboard에 recent 추가**

`getDashboard` 반환 객체에 `recent` 추가 (기존 키는 유지):

```js
    const recent = rows(await db.query(buildKeysetUnionQuery(null, 10).sql, buildKeysetUnionQuery(null, 10).params, []));
    // ↑ 간결하게: const rq = buildKeysetUnionQuery(null, 10); const recent = rows(await db.query(rq.sql, rq.params));

    return {
      car_birth:        carBirth,
      total_days:       totalDays,
      recent,
      recent_fuel:      recentFuel,
      last_maintenance: lastMaintenance,
      last_other:       lastOther,
      cost_last_30d:    fuel30d + maint30d + other30d,
      avg_economy:      avgRow?.avg ? Math.round(avgRow.avg * 100) / 100 : null,
      latest_odometer:  odomRow?.odometer ?? null,
    };
```

- [ ] **Step 7: 전체 테스트 + 빌드 확인**

Run: `cd frontend && npm test && npm run build`
Expected: 모든 테스트 PASS, 빌드 성공. (`FuelList`/`MaintenanceList`/`OtherList`가 아직 `getFuelPage` 등을 import → **빌드 깨짐**. 이 경우 Step 8로.)

- [ ] **Step 8: 구 리스트의 임시 호환**

`FuelList.jsx`/`MaintenanceList.jsx`/`OtherList.jsx`는 Task 6·10에서 삭제될 예정이나, 이 task 시점에 빌드가 깨지면 안 된다. 각 파일의 `usePaginatedList(api.getFuelPage)` 를 `usePaginatedList((opts) => api.getRecordsPage({ ...opts, filter: 'fuel' }))` 로 한 줄 교체 (maintenance/other 동일).

Run: `cd frontend && npm run build`
Expected: 빌드 성공.

- [ ] **Step 9: 커밋**

```bash
git add frontend/src/api.js frontend/src/records-pagination.integration.test.js frontend/src/pages/FuelList.jsx frontend/src/pages/MaintenanceList.jsx frontend/src/pages/OtherList.jsx
git commit -m "feat: 통합 기록 조회 API (getRecordsPage, getRecentRecords)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 4: SegmentTabs 컴포넌트

**Files:**
- Create: `frontend/src/components/SegmentTabs.jsx`
- Modify: `frontend/src/index.css`

**Interfaces:**
- Produces:
  ```
  <SegmentTabs
    options={[{ key: string, label: string, Icon?: SvgComponent }]}
    value={string | null}
    onChange={(key: string) => void}   // allowDeselect=true & 선택된 탭 재클릭 → onChange(null)
    allowDeselect={boolean}
    className={string}                  // 위치 컨텍스트용 ('records-tabs' | 'form-tabs')
  />
  ```
- 순수 프레젠테이션. 상태는 부모가 소유.

- [ ] **Step 1: 컴포넌트 작성**

`frontend/src/components/SegmentTabs.jsx`:

```jsx
export default function SegmentTabs({ options, value, onChange, allowDeselect = false, className = '' }) {
  const handle = (key) => {
    if (key === value) {
      if (allowDeselect) onChange(null)
      return
    }
    onChange(key)
  }
  return (
    <div className={`segment-tabs ${className}`}>
      {options.map(({ key, label, Icon }) => (
        <button
          key={key}
          type="button"
          className={'segment-tab' + (key === value ? ' active' : '')}
          onClick={() => handle(key)}
        >
          {Icon && <Icon />}
          <span>{label}</span>
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: CSS 추가**

`frontend/src/index.css`에 추가 (기존 `.bottom-nav`/`.nav-item` 블록 근처, 색·반경 토큰 재사용):

```css
/*** 세그먼트 탭 (기록 필터 / 양식 전환 공유) ***/
.segment-tabs {
  display: flex; gap: 4px; padding: 6px;
  background: var(--surface); z-index: 90;
}
.segment-tabs.records-tabs {
  position: sticky; top: calc(var(--safe-top) + 8px);
}
.segment-tabs.form-tabs {
  position: sticky; top: calc(var(--safe-top) + 56px);
}
.segment-tab {
  flex: 1; display: flex; align-items: center; justify-content: center; gap: 5px;
  padding: 8px 0; border: none; border-radius: var(--radius-sm);
  background: var(--surface-elevated); color: var(--on-surface-variant);
  font-size: 13px; font-weight: 500;
}
.segment-tab svg { width: 16px; height: 16px; }
.segment-tab.active { background: var(--accent); color: #fff; }
```

- [ ] **Step 3: 린트 + 빌드**

Run: `cd frontend && npm run lint && npm run build`
Expected: 통과.

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/components/SegmentTabs.jsx frontend/src/index.css
git commit -m "feat: SegmentTabs 공유 컴포넌트

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 5: 기록 카드 컴포넌트 3종

**Files:**
- Create: `frontend/src/components/records/recordType.js`, `frontend/src/components/records/FuelCard.jsx`, `frontend/src/components/records/MaintenanceCard.jsx`, `frontend/src/components/records/OtherCard.jsx`

**Interfaces:**
- Consumes: Task 1 아이콘 (`fuel.svg`, `charge.svg`, `maintenance.svg`, `other.svg`, `kebab.svg`), 기존 `MoreMenu`, `useSettings`.
- Produces:
  - `recordType.js`: `getRecordType(src, fuelTerm) => { label: string, Icon: SvgComponent }`
  - `<FuelCard record={row} showTypeIcon={bool} onEdit={fn?} onDelete={fn?} />`
  - `<MaintenanceCard record onEdit? onDelete? showTypeIcon? />`
  - `<OtherCard record onEdit? onDelete? showTypeIcon? />`
  - `onEdit`/`onDelete` 미전달 시 `MoreMenu` 렌더 안 함 (Home용).

- [ ] **Step 1: recordType.js**

```js
import FuelIcon        from '../../assets/symbols/fuel.svg?react'
import ChargeIcon      from '../../assets/symbols/charge.svg?react'
import MaintenanceIcon from '../../assets/symbols/maintenance.svg?react'
import OtherIcon       from '../../assets/symbols/other.svg?react'

export function getRecordType(src, fuelTerm = '주유') {
  if (src === 'fuel')
    return { label: fuelTerm, Icon: fuelTerm === '충전' ? ChargeIcon : FuelIcon }
  if (src === 'maintenance') return { label: '정비', Icon: MaintenanceIcon }
  return { label: '기타', Icon: OtherIcon }
}
```

- [ ] **Step 2: FuelCard.jsx**

`FuelList.jsx`의 카드 마크업 + 데이터 이상 판정 로직을 옮긴다:

```jsx
import MoreMenu from '../MoreMenu'
import { useSettings } from '../../context/SettingsContext'
import { getRecordType } from './recordType'

const fmt = (n) => Number(n).toLocaleString('ko-KR')

export default function FuelCard({ record: r, showTypeIcon = false, onEdit, onDelete }) {
  const { options, settings, fuelTerm } = useSettings()
  const economyUnit = options.car_fuel.find(o => o.code === settings.car_fuel)?.economy_unit ?? 'km/L'
  const { Icon } = getRecordType('fuel', fuelTerm)

  const badOdometer = !r.odometer || r.odometer <= 0
  const suspiciousInterval = r.interval_km && r.odometer && r.interval_km >= r.odometer * 0.95
  const hasDataIssue = badOdometer || suspiciousInterval
  const showEconomy = r.fuel_economy && !hasDataIssue && r.fuel_economy <= 50

  return (
    <div className="card">
      <div className="card-row">
        {showTypeIcon && <div className="card-icon"><Icon /></div>}
        <div>
          <div className="card-title">{r.date}</div>
          <div className="card-sub">
            {fmt(r.odometer)}km{r.interval_km ? ` · +${r.interval_km}km` : ''}
            {hasDataIssue && <>&nbsp;<span className="badge orange">주행거리 오류</span></>}
          </div>
        </div>
        <div className="card-amount">
          {fmt(r.amount)}원
          {(onEdit || onDelete) && <MoreMenu onEdit={onEdit} onDelete={onDelete} />}
        </div>
      </div>
      <div className="card-meta">
        {r.liters      && <span>{parseFloat(r.liters).toLocaleString('ko-KR', { maximumFractionDigits: 3 })}L</span>}
        {r.unit_price  && <span>@{fmt(r.unit_price)}원/L</span>}
        {showEconomy   && <span className="badge green">{r.fuel_economy}&nbsp;{economyUnit}</span>}
        {r.memo        && <span>{r.memo}</span>}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: MaintenanceCard.jsx**

`MaintenanceList.jsx` 카드 마크업 이식:

```jsx
import MoreMenu from '../MoreMenu'
import { getRecordType } from './recordType'

const fmt = (n) => Number(n).toLocaleString('ko-KR')

export default function MaintenanceCard({ record: r, showTypeIcon = false, onEdit, onDelete }) {
  const { Icon } = getRecordType('maintenance')
  return (
    <div className="card">
      <div className="card-row">
        {showTypeIcon && <div className="card-icon"><Icon /></div>}
        <div>
          <div className="card-title">{r.item}</div>
          <div className="card-sub">{r.date} · {fmt(r.odometer)}km</div>
        </div>
        <div className="card-amount">
          {r.amount ? `${fmt(r.amount)}원` : '-'}
          {(onEdit || onDelete) && <MoreMenu onEdit={onEdit} onDelete={onDelete} />}
        </div>
      </div>
      {r.memo && <div className="card-meta"><span>{r.memo}</span></div>}
    </div>
  )
}
```

- [ ] **Step 4: OtherCard.jsx**

`OtherList.jsx` 카드 마크업 이식:

```jsx
import MoreMenu from '../MoreMenu'
import { getRecordType } from './recordType'

const fmt = (n) => Number(n).toLocaleString('ko-KR')

export default function OtherCard({ record: r, showTypeIcon = false, onEdit, onDelete }) {
  const { Icon } = getRecordType('other')
  return (
    <div className="card">
      <div className="card-row">
        {showTypeIcon && <div className="card-icon"><Icon /></div>}
        <div>
          <div className="card-title">{r.item}</div>
          <div className="card-sub">{r.date}{r.odometer ? ` · ${fmt(r.odometer)}km` : ''}</div>
        </div>
        <div className="card-amount">
          {r.amount ? `${fmt(r.amount)}원` : '-'}
          {(onEdit || onDelete) && <MoreMenu onEdit={onEdit} onDelete={onDelete} />}
        </div>
      </div>
      {r.memo && <div className="card-meta"><span>{r.memo}</span></div>}
    </div>
  )
}
```

- [ ] **Step 5: 린트 + 빌드**

Run: `cd frontend && npm run lint && npm run build`
Expected: 통과. (아직 아무도 import 안 하므로 tree-shake됨 — 정상.)

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/components/records
git commit -m "feat: 기록 카드 컴포넌트 3종 추출 (Fuel/Maintenance/Other)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 6: Records 페이지 (통합 리스트 + 필터 탭)

**Files:**
- Create: `frontend/src/pages/Records.jsx`
- Modify: `frontend/src/App.jsx` (라우트 1개 추가), `frontend/src/index.css` (`.content.no-topbar`)

**Interfaces:**
- Consumes: Task 3 `api.getRecordsPage`, Task 4 `SegmentTabs`, Task 5 카드 3종, 기존 `usePaginatedList`, `useSettings`, `useNavigate`.
- Produces: `/records` 라우트가 `<Records />` 렌더.

- [ ] **Step 1: Records.jsx 작성**

```jsx
import { useState, useCallback } from 'react'
import { useNavigate } from 'react-router-dom'
import { api } from '../api'
import { useSettings } from '../context/SettingsContext'
import { usePaginatedList } from '../hooks/usePaginatedList'
import SegmentTabs from '../components/SegmentTabs'
import FuelCard from '../components/records/FuelCard'
import MaintenanceCard from '../components/records/MaintenanceCard'
import OtherCard from '../components/records/OtherCard'
import { getRecordType } from '../components/records/recordType'
import FuelIcon        from '../assets/symbols/fuel.svg?react'
import ChargeIcon      from '../assets/symbols/charge.svg?react'
import MaintenanceIcon from '../assets/symbols/maintenance.svg?react'
import OtherIcon       from '../assets/symbols/other.svg?react'

const CARD = { fuel: FuelCard, maintenance: MaintenanceCard, other: OtherCard }
const EDIT_PATH = {
  fuel: (id) => `/records/fuel/${id}/edit`,
  maintenance: (id) => `/records/maintenance/${id}/edit`,
  other: (id) => `/records/other/${id}/edit`,
}
const DELETE_API = { fuel: api.deleteFuel, maintenance: api.deleteMaintenance, other: api.deleteOther }

function RecordsList({ filter }) {
  const navigate = useNavigate()
  const fetchPage = useCallback(
    ({ cursor }) => api.getRecordsPage({ cursor, filter }),
    [filter]
  )
  const { records, hasMore, sentinelRef, removeRecord } = usePaginatedList(fetchPage)

  const handleDelete = async (src, id) => {
    if (!window.confirm('삭제할까요?')) return
    await DELETE_API[src](id)
    removeRecord(id)
  }

  if (records.length === 0 && !hasMore)
    return <div className="empty">기록이 없어요</div>

  return (
    <>
      {records.map(r => {
        const Card = CARD[r.src]
        return (
          <Card
            key={`${r.src}-${r.id}`}
            record={r}
            showTypeIcon
            onEdit={() => navigate(EDIT_PATH[r.src](r.id))}
            onDelete={() => handleDelete(r.src, r.id)}
          />
        )
      })}
      {hasMore && <div ref={sentinelRef} style={{ height: 1 }} />}
    </>
  )
}

export default function Records() {
  const navigate = useNavigate()
  const { fuelTerm } = useSettings()
  const [filter, setFilter] = useState(null)

  const options = [
    { key: 'fuel', label: fuelTerm, Icon: fuelTerm === '충전' ? ChargeIcon : FuelIcon },
    { key: 'maintenance', label: '정비', Icon: MaintenanceIcon },
    { key: 'other', label: '기타', Icon: OtherIcon },
  ]

  return (
    <>
      <button className="btn-add" onClick={() => navigate('/records/new')}>+</button>
      <div className="content no-topbar">
        <SegmentTabs
          className="records-tabs"
          options={options}
          value={filter}
          onChange={setFilter}
          allowDeselect
        />
        <RecordsList key={filter ?? 'all'} filter={filter} />
      </div>
    </>
  )
}
```

- [ ] **Step 2: CSS — no-topbar 콘텐츠**

`frontend/src/index.css`의 `.content` 규칙 아래에 추가:

```css
.content.no-topbar { padding-top: calc(var(--safe-top) + 8px); }
```

- [ ] **Step 3: App.jsx에 라우트 추가**

`frontend/src/App.jsx` import에 `import Records from './pages/Records'` 추가, `<Routes>` 안에 (기존 `/fuel` 라우트들은 이 task에서 **그대로 둔다** — Task 10에서 정리):

```jsx
        <Route path="/records" element={<Records />} />
```

- [ ] **Step 4: 린트 + 빌드**

Run: `cd frontend && npm run lint && npm run build`
Expected: 통과.

- [ ] **Step 5: 수동 확인**

`npm run dev` 후 `/records`:
- [ ] 전체 목록이 최신순으로 뜬다 (주유·정비·기타 혼재, 좌측 타입 아이콘)
- [ ] "주유" 탭 → 주유만, 다시 "주유" 탭 → 전체 복귀
- [ ] 스크롤 시 탭이 상단에 고정(sticky)
- [ ] 카드 더보기 → 수정(해당 편집 경로로 이동, 아직 구 양식) / 삭제(confirm 후 사라짐)
- [ ] 무한 스크롤로 다음 페이지가 이어 붙는다 (기록 50건 이상일 때)
- [ ] `+` 버튼 → `/records/new` (아직 404 또는 미구현 — 정상, Task 8에서)

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/pages/Records.jsx frontend/src/App.jsx frontend/src/index.css
git commit -m "feat: 통합 기록 페이지 (필터 탭 + 무한 스크롤)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 7: 양식 컴포넌트 리팩터 (forwardRef + mode)

**Files:**
- Modify: `frontend/src/pages/FuelForm.jsx`, `frontend/src/pages/MaintenanceForm.jsx`, `frontend/src/pages/OtherForm.jsx`

**Interfaces:**
- Produces: 세 폼 모두
  ```
  const Form = forwardRef(function Form({ mode = 'standalone' }, ref) → JSX)
  // ref: { submit: () => Promise<void>, isDirty: () => boolean }
  // mode 'standalone': 자체 topbar(✕/저장) 렌더, 성공/취소 시 navigate('/records')
  // mode 'embedded': topbar 없음, 저장/취소는 부모(RecordForm)가 ref로 관장
  ```
- Consumes: 기존 `api.createFuel`/`updateFuel` 등, `useParams`, `useNavigate`.

- [ ] **Step 1: FuelForm 리팩터**

`frontend/src/pages/FuelForm.jsx` 전체 교체:

```jsx
import { useState, useEffect, useRef, forwardRef, useImperativeHandle } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { api } from '../api'

const BLANK = {
  date: new Date().toISOString().split('T')[0],
  type: '가득주유', amount: '', unit_price: '', liters: '', odometer: '', memo: '',
}

const FuelForm = forwardRef(function FuelForm({ mode = 'standalone' }, ref) {
  const { id } = useParams()
  const navigate = useNavigate()
  const isEdit = Boolean(id)

  const [form, setForm] = useState(BLANK)
  const [initial, setInitial] = useState(null)

  useEffect(() => {
    if (isEdit) {
      api.getFuel().then(records => {
        const r = records.find(r => r.id === Number(id))
        if (r) {
          const next = {
            date: r.date, type: r.type,
            amount: r.amount ?? '', unit_price: r.unit_price ?? '',
            liters: r.liters ?? '', odometer: r.odometer ?? '', memo: r.memo ?? '',
          }
          setForm(next); setInitial(next)
        }
      })
    } else {
      api.getFuel().then(records => {
        const next = records.length > 0 ? { ...BLANK, odometer: records[0].odometer } : BLANK
        setForm(next); setInitial(next)
      })
    }
  }, [id])

  const set = (key, val) => setForm(f => ({ ...f, [key]: val }))

  const calc = (changed) => {
    const a = parseFloat(form.amount), u = parseFloat(form.unit_price), l = parseFloat(form.liters)
    if (changed === 'amount' || changed === 'unit_price') { if (a && u) set('liters', (a / u).toFixed(2)) }
    else if (changed === 'liters') {
      if (a && l) set('unit_price', Math.round(a / l))
      else if (u && l) set('amount', Math.round(u * l))
    }
  }

  const doSubmit = async () => {
    const body = {
      date: form.date, type: form.type, amount: Number(form.amount),
      unit_price: form.unit_price ? Number(form.unit_price) : null,
      liters: form.liters ? Number(form.liters) : null,
      odometer: Number(form.odometer), memo: form.memo || null,
    }
    if (isEdit) await api.updateFuel(id, body)
    else await api.createFuel(body)
    navigate('/records')
  }

  useImperativeHandle(ref, () => ({
    submit: doSubmit,
    isDirty: () => initial != null && JSON.stringify(form) !== JSON.stringify(initial),
  }), [form, initial])

  const handleSubmit = (e) => { e.preventDefault(); doSubmit() }

  return (
    <form onSubmit={handleSubmit}>
      {mode === 'standalone' && (
        <>
          <div className="topbar">
            <button type="button" className="btn-cancel" aria-label="취소" onClick={() => navigate('/records')}>✕</button>
            <button type="submit" className="btn-submit" aria-label="저장"></button>
          </div>
          <div className="topbg"></div>
        </>
      )}
      <div className={mode === 'standalone' ? 'content' : 'content no-topbar'}>

        <div className="form-group">
          <label>날짜</label>
          <input type="date" required value={form.date} onChange={e => set('date', e.target.value)} />
        </div>

        <div className="form-group">
          <label>구분</label>
          <div className="select-wrap">
            <select value={form.type} onChange={e => set('type', e.target.value)}>
              <option>가득주유</option>
              <option>부분주유</option>
            </select>
          </div>
        </div>

        <div className="form-group">
          <label>금액 (원)</label>
          <input type="number" inputMode="numeric" required
            value={form.amount} onChange={e => set('amount', e.target.value)} onBlur={() => calc('amount')} />
        </div>

        <div className="form-group">
          <label>단가 (원/L)</label>
          <input type="number" inputMode="numeric"
            value={form.unit_price} onChange={e => set('unit_price', e.target.value)} onBlur={() => calc('unit_price')} />
        </div>

        <div className="form-group">
          <label>주유량 (L)</label>
          <input type="number" step="0.001" inputMode="decimal"
            value={form.liters} onChange={e => set('liters', e.target.value)} onBlur={() => calc('liters')} />
        </div>

        <div className="form-group">
          <label>누적 주행거리 (km)</label>
          <input type="number" inputMode="numeric" required
            value={form.odometer} onChange={e => set('odometer', e.target.value)} />
        </div>

        <div className="form-group">
          <label>메모</label>
          <textarea value={form.memo} onChange={e => set('memo', e.target.value)} />
        </div>

      </div>
    </form>
  )
})

export default FuelForm
```

- [ ] **Step 2: MaintenanceForm 리팩터**

같은 패턴 적용. `BLANK = { date: 오늘, item: '', amount: '0', odometer: '', memo: '' }`. `useEffect`는 기존처럼 `Promise.all([api.getMaintenanceItems(), isEdit ? api.getMaintenance() : null, api.getFuel()])` — 로딩 후 `setForm(next); setInitial(next)` (신규는 `item: itemList[0]`, `odometer: fuelRecords[0]?.odometer ?? ''` 반영). `doSubmit`은 기존 `handleSubmit` 본문에서 `navigate('/maintenance')` → `navigate('/records')`. `useImperativeHandle`·`mode` 분기·`content` 클래스는 Step 1과 동일 구조. 폼 필드 마크업(날짜/항목/금액/주행거리/메모)은 기존 그대로.

- [ ] **Step 3: OtherForm 리팩터**

같은 패턴. `BLANK = { date: 오늘, item: '', amount: '', odometer: '', memo: '' }`. `doSubmit`의 `navigate('/other')` → `navigate('/records')`. 나머지 동일.

- [ ] **Step 4: App.jsx 편집 라우트 경로 변경**

`frontend/src/App.jsx`에서 편집 라우트 경로만 교체 (컴포넌트는 그대로, `mode` 기본값 standalone):

```jsx
        <Route path="/records/fuel/:id/edit"        element={<FuelForm />} />
        <Route path="/records/maintenance/:id/edit" element={<MaintenanceForm />} />
        <Route path="/records/other/:id/edit"       element={<OtherForm />} />
```

기존 `/fuel/:id/edit` 등 3줄은 이 task에서 삭제(신규 `/fuel/new` 등과 리스트 라우트는 Task 10까지 유지). `/fuel/new` 등은 아직 남으므로 `FuelForm` import도 유지.

- [ ] **Step 5: 린트 + 빌드**

Run: `cd frontend && npm run lint && npm run build`
Expected: 통과.

- [ ] **Step 6: 수동 확인**

`npm run dev`:
- [ ] `/records`에서 주유 카드 더보기 → 수정 → `/records/fuel/:id/edit` 편집 화면(자체 ✕/저장 바), 저장 시 `/records` 복귀
- [ ] 정비·기타도 동일
- [ ] 편집 화면에서 값 안 바꾸고 저장해도 정상

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/pages/FuelForm.jsx frontend/src/pages/MaintenanceForm.jsx frontend/src/pages/OtherForm.jsx frontend/src/App.jsx
git commit -m "refactor: 입력 양식을 forwardRef + mode(standalone/embedded)로

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 8: RecordForm 래퍼 (신규 입력 세그먼트 탭)

**Files:**
- Create: `frontend/src/pages/RecordForm.jsx`
- Modify: `frontend/src/App.jsx` (`/records/new` 라우트)

**Interfaces:**
- Consumes: Task 4 `SegmentTabs`, Task 7 폼 3종(ref: `{ submit, isDirty }`), `useSettings`, `useNavigate`.
- Produces: `/records/new` → `<RecordForm />`.

- [ ] **Step 1: RecordForm.jsx 작성**

```jsx
import { useState, useRef } from 'react'
import { useNavigate } from 'react-router-dom'
import { useSettings } from '../context/SettingsContext'
import SegmentTabs from '../components/SegmentTabs'
import FuelForm from './FuelForm'
import MaintenanceForm from './MaintenanceForm'
import OtherForm from './OtherForm'
import FuelIcon        from '../assets/symbols/fuel.svg?react'
import ChargeIcon      from '../assets/symbols/charge.svg?react'
import MaintenanceIcon from '../assets/symbols/maintenance.svg?react'
import OtherIcon       from '../assets/symbols/other.svg?react'

const FORM = { fuel: FuelForm, maintenance: MaintenanceForm, other: OtherForm }

export default function RecordForm() {
  const navigate = useNavigate()
  const { fuelTerm } = useSettings()
  const [tab, setTab] = useState('fuel')
  const formRef = useRef(null)

  const options = [
    { key: 'fuel', label: fuelTerm, Icon: fuelTerm === '충전' ? ChargeIcon : FuelIcon },
    { key: 'maintenance', label: '정비', Icon: MaintenanceIcon },
    { key: 'other', label: '기타', Icon: OtherIcon },
  ]

  const handleTab = (key) => {
    if (key === tab) return
    if (formRef.current?.isDirty() && !window.confirm('입력 중인 내용을 버리고 이동할까요?')) return
    setTab(key)
  }

  const Form = FORM[tab]

  return (
    <div>
      <div className="topbar">
        <button type="button" className="btn-cancel" aria-label="취소" onClick={() => navigate('/records')}>✕</button>
        <button type="button" className="btn-submit" aria-label="저장" onClick={() => formRef.current?.submit()}></button>
      </div>
      <div className="topbg"></div>
      <SegmentTabs className="form-tabs" options={options} value={tab} onChange={handleTab} allowDeselect={false} />
      <Form key={tab} ref={formRef} mode="embedded" />
    </div>
  )
}
```

- [ ] **Step 2: App.jsx 라우트**

`import RecordForm from './pages/RecordForm'` 추가. `<Routes>`에:

```jsx
        <Route path="/records/new" element={<RecordForm />} />
```

- [ ] **Step 3: 린트 + 빌드**

Run: `cd frontend && npm run lint && npm run build`
Expected: 통과.

- [ ] **Step 4: 수동 확인**

`npm run dev`, `/records` → `+`:
- [ ] 상단에 ✕/저장 고정 바 + 그 아래 주유/정비/기타 탭, 기본 "주유"
- [ ] 주유 양식에 값 입력 → "정비" 탭 클릭 → "버리고 이동?" 확인창, 취소 시 주유 유지
- [ ] 아무것도 안 건드리고 탭 이동 → 확인창 없이 전환
- [ ] 정비 탭에서 저장 → 정비 기록 생성, `/records` 복귀
- [ ] EV 차량 설정 시 첫 탭 라벨이 "충전", 아이콘 🔌

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/pages/RecordForm.jsx frontend/src/App.jsx
git commit -m "feat: 신규 입력 세그먼트 탭 래퍼 (RecordForm)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 9: Home 재작성

**Files:**
- Modify: `frontend/src/pages/Home.jsx`, `frontend/src/index.css`, `frontend/src/api.js` (getDashboard 정리)

**Interfaces:**
- Consumes: Task 3 `api.getDashboard().recent`, Task 5 카드 3종 + `getRecordType`, Task 1 `car.svg`, 기존 사진 업로드 모달 로직·`useSettings`.
- Produces: 없음(말단 페이지).

- [ ] **Step 1: api.js getDashboard 정리**

`recent_fuel`, `last_maintenance`, `last_other` 및 이를 위한 쿼리(`recentFuel`, `lastMaintenance`, `lastOther`) 제거. 반환 객체는 `recent`만 유지. `getFuel`/`getMaintenance`/`getOther` 전체조회 함수는 그대로 둔다.

- [ ] **Step 2: Home.jsx 재작성**

핵심 변경:
- 최상단 `topbar topbar-home`(`carThumb` + `h1 환영합니다` + gear NavLink) **삭제**.
- `topbg` 삭제. 하단 `section-footer`(안전운전 + 버전) **삭제**. `VERSION` import 제거.
- 콘텐츠 최상단에 **정체성 블록**:
  ```jsx
  <div className="identity-block">
    {carThumb}
    <div className="identity-text">
      <div className="identity-name">{settings.car_brand} {settings.car_model}</div>
      <div className="identity-spec">
        {[settings.car_plate, settings.car_birth?.slice(0,4), carTypeLabel, carFuelLabel].filter(Boolean).join(' · ')}
      </div>
    </div>
  </div>
  ```
- `carThumb`의 fallback 아이콘: `ProfileIcon` → `import CarIcon from '../assets/symbols/car.svg?react'`.
- **요약 타일** (가로 줄 3개):
  ```jsx
  <div className="summary-tiles">
    <div className="summary-tile"><span>한 달 지출</span><span><b>{fmt(data.cost_last_30d ?? 0)}</b> 원</span></div>
    <div className="summary-tile"><span>평균 {economyLabel}</span><span><b>{data.avg_economy ?? '-'}</b> {economyUnit}</span></div>
    <div className="summary-tile"><span>총 주행거리</span><span><b>{data.latest_odometer ? fmt(data.latest_odometer) : '-'}</b> km</span></div>
  </div>
  ```
- **최근 기록**: `data.recent`가 있으면 `section-header "최근 기록"` + 매핑:
  ```jsx
  {data.recent?.length > 0 && (
    <>
      <div className="section-header">최근 기록</div>
      {data.recent.map(r => {
        const Card = { fuel: FuelCard, maintenance: MaintenanceCard, other: OtherCard }[r.src]
        return <Card key={`${r.src}-${r.id}`} record={r} showTypeIcon />
      })}
    </>
  )}
  ```
  (`onEdit`/`onDelete` 미전달 → Home 카드엔 더보기 없음.)
- `.content`는 `className="content no-topbar"`.
- 사진 업로드/크롭/삭제 모달(`carImageModal`, `cropModal`, `handleFileSelect` 등)과 `data === null` 로딩 분기는 유지하되, 로딩 분기도 topbar 없는 새 레이아웃으로 통일(정체성 블록 + `<div className="empty">불러오는 중…</div>`).
- 기존 개별 아이콘 import(`FuelIcon`, `MaintenanceIcon`, `OtherIcon`, `SettingsIcon`, `HomeIcon`, `ProfileIcon`) 중 안 쓰는 것 제거.

- [ ] **Step 3: CSS 추가/제거**

`frontend/src/index.css`:
- 제거: `.car-profile h1`, `.topbar-home`, `.topbar-home h1` 및 `.car-profile` 계열 중 미사용 규칙.
- 추가:
  ```css
  .identity-block { display: flex; align-items: center; gap: 13px; padding: 4px 2px 16px; }
  .identity-name { font-size: 17px; font-weight: 600; }
  .identity-spec { font-size: 12px; color: var(--on-surface-variant); margin-top: 2px; }
  .summary-tiles { display: flex; flex-direction: column; gap: 6px; margin-bottom: 8px; }
  .summary-tile {
    display: flex; justify-content: space-between; align-items: baseline;
    background: var(--surface-elevated); border: var(--outline);
    border-radius: var(--radius-sm); padding: 9px 14px;
  }
  .summary-tile > span:first-child { font-size: 12px; color: var(--on-surface-variant); }
  .summary-tile b { font-size: 15px; font-weight: 700; }
  .summary-tile > span:last-child { font-size: 11px; color: var(--on-surface-variant); }
  ```
  (토큰 확인됨: `--surface-elevated`, `--outline`, `--radius-sm`(10px), `--on-surface-variant` 모두 index.css `:root`에 실재. `--radius-md`는 없음 → `--radius-sm` 사용.)

- [ ] **Step 4: 린트 + 빌드 + 전체 테스트**

Run: `cd frontend && npm run lint && npm test && npm run build`
Expected: 통과.

- [ ] **Step 5: 수동 확인**

- [ ] Home 상단: 고정 헤더 없음, 프로필 사진 + "현대 아반떼" + "123가4567 · 2021 · 준중형 · 휘발유"
- [ ] 사진 탭 → 업로드/편집/삭제 모달 정상
- [ ] 요약 타일 3줄, 값 없으면 "-"
- [ ] 최근 기록 10개 이하, 최신순, 타입 아이콘, 더보기 없음
- [ ] 기록 0건이면 "최근 기록" 헤더 안 뜸
- [ ] 하단에 안전운전/버전 문구 없음

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/pages/Home.jsx frontend/src/index.css frontend/src/api.js
git commit -m "feat: Home 재구성 (정체성 블록 + 요약 타일 + 통합 최근기록, topbar 제거)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Task 10: 라우팅 재편 + BottomNav + Settings + 정리

**Files:**
- Modify: `frontend/src/App.jsx`, `frontend/src/components/BottomNav.jsx`, `frontend/src/components/MoreMenu.jsx`, `frontend/src/pages/Settings.jsx`, `frontend/src/index.css`
- Delete: `frontend/src/pages/FuelList.jsx`, `frontend/src/pages/MaintenanceList.jsx`, `frontend/src/pages/OtherList.jsx`, `frontend/src/assets/symbols/oil_and_electric.svg`, `frontend/src/assets/symbols/gear.svg`, `frontend/src/assets/symbols/download.svg`, `frontend/src/assets/symbols/upload.svg`, `frontend/src/assets/defaults/profile.svg`

**Interfaces:**
- Consumes: Task 1 아이콘(`home`, `records`, `settings`, `import`, `export`, `copy`, `kebab`), Task 6 `Records`, Task 8 `RecordForm`.
- Produces: 최종 라우트 셋.

- [ ] **Step 1: App.jsx 라우팅 재작성**

`<Routes>`를 다음으로 정리 (구 `/fuel*`, `/maintenance*`, `/other*` 전부 제거):

```jsx
        <Route path="/onboarding"                   element={<Onboarding />} />
        <Route path="/"                             element={<Home />} />
        <Route path="/records"                      element={<Records />} />
        <Route path="/records/new"                  element={<RecordForm />} />
        <Route path="/records/fuel/:id/edit"        element={<FuelForm />} />
        <Route path="/records/maintenance/:id/edit" element={<MaintenanceForm />} />
        <Route path="/records/other/:id/edit"       element={<OtherForm />} />
        <Route path="/settings"                     element={<Settings />} />
        <Route path="/import"                       element={<ImportGuide />} />
        <Route path="/feedback"                     element={<Feedback />} />
```

`FuelList`/`MaintenanceList`/`OtherList` import 삭제. `FuelForm`/`MaintenanceForm`/`OtherForm` import는 유지(편집 라우트).

- [ ] **Step 2: BottomNav 3링크**

`frontend/src/components/BottomNav.jsx` 전체 교체:

```jsx
import { NavLink } from 'react-router-dom'
import HomeIcon     from '../assets/symbols/home.svg?react'
import RecordsIcon  from '../assets/symbols/records.svg?react'
import SettingsIcon from '../assets/symbols/settings.svg?react'

export default function BottomNav() {
  const cls = ({ isActive }) => 'nav-item' + (isActive ? ' active' : '')
  return (
    <nav className="bottom-nav">
      <NavLink to="/" end className={cls}><HomeIcon />홈</NavLink>
      <NavLink to="/records" className={cls}><RecordsIcon />기록</NavLink>
      <NavLink to="/settings" className={cls}><SettingsIcon />설정</NavLink>
    </nav>
  )
}
```

- [ ] **Step 3: MoreMenu 케밥 아이콘**

`frontend/src/components/MoreMenu.jsx`의 `import OtherIcon from '../assets/symbols/other.svg?react'` → `import KebabIcon from '../assets/symbols/kebab.svg?react'`, 사용처 `<OtherIcon />` → `<KebabIcon />`.

- [ ] **Step 4: Settings — 버전 표기 + 아이콘**

`frontend/src/pages/Settings.jsx`:
- `import { VERSION } from '../version.js'` 추가.
- `import DownloadIcon from '../assets/symbols/download.svg?react'` → `'../assets/symbols/import.svg?react'` (이름 `ImportIcon`), `UploadIcon` → `'../assets/symbols/export.svg?react'` (`ExportIcon`). 사용처 태그명 함께 변경.
- `copy` 아이콘을 쓰는 곳이 있으면 `'../assets/symbols/copy.svg?react'` 유지(파일은 Task 1에서 재생성됨).
- `<div className="content">` 최하단(`기타` 섹션 뒤)에 추가:
  ```jsx
  <div className="section-footer">차필(chapil) v{VERSION}</div>
  ```

- [ ] **Step 5: CSS 정리**

`frontend/src/index.css`:
- `.bottom-nav { width: 275px; }` → 3칸 기준으로 조정 (예: `width: 240px;` — 실측 후 확정).
- `.nav-item svg`가 새 이모지 SVG에서 26px로 뭉개지면 `.segment-tab svg`처럼 크기만 유지, 필요 시 `extract-emoji.py`의 `PAD`를 0.04로 낮추고 `npm run icons` 재실행(별도 커밋).

- [ ] **Step 6: 구 파일 삭제**

```bash
cd frontend
git rm src/pages/FuelList.jsx src/pages/MaintenanceList.jsx src/pages/OtherList.jsx
git rm src/assets/symbols/oil_and_electric.svg src/assets/symbols/gear.svg src/assets/symbols/download.svg src/assets/symbols/upload.svg
git rm src/assets/defaults/profile.svg
```

- [ ] **Step 7: 전수 검색으로 잔여 참조 확인**

Run:
```bash
cd frontend && grep -rn "FuelList\|MaintenanceList\|OtherList\|oil_and_electric\|symbols/gear\|symbols/download\|symbols/upload\|defaults/profile\|getFuelPage\|getMaintenancePage\|getOtherPage\|/fuel'\|/maintenance'\|/other'\|topbar-home\|recent_fuel\|last_maintenance\|last_other" src
```
Expected: 결과 없음 (또는 주석뿐). 있으면 해당 파일 수정.

- [ ] **Step 8: 린트 + 전체 테스트 + 빌드**

Run: `cd frontend && npm run lint && npm test && npm run build`
Expected: 전부 통과.

- [ ] **Step 9: 전체 수동 체크리스트**

`npm run dev`:
- [ ] BottomNav 3칸(홈/기록/설정), 아이콘 26px에서 판별 가능, active 색(`--accent`) 적용
- [ ] 다크 모드에서 아이콘 색 정상
- [ ] 홈 → 기록 → 설정 이동, 각 화면 상단 고정 헤더 유무가 스펙대로 (홈·기록 없음, 양식·설정 있음)
- [ ] `/records/new`에서 3종 입력·저장, dirty 확인
- [ ] 기록 카드 수정/삭제
- [ ] 구 경로 `/fuel` 직접 입력 시 빈 화면(라우트 없음) — 의도된 하드 컷오버
- [ ] 설정 최하단 `차필(chapil) v<버전>` 표기
- [ ] 데이터 가져오기/내보내기 버튼 아이콘 정상
- [ ] `npm run icons` 재실행해도 git diff 없음(멱등)

- [ ] **Step 10: 커밋**

```bash
git add -A
git commit -m "feat: 내비게이션 3분할 + 라우팅 재편 + 구 리스트/아이콘 정리

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_017FuEYjVNB1ybZ335NHr3SK"
```

---

## Self-Review

**스펙 커버리지:**
- §2 아이콘 시스템 → Task 1 (파이프라인·매핑·케밥 예외·vendoring). §2.4 교체·폐기 → Task 1(재생성) + Task 9(profile) + Task 10(구 파일 삭제).
- §3 내비게이션/라우팅 → Task 6·7·8(라우트 추가) + Task 10(재편·BottomNav·topbar 처리).
- §4 Home → Task 9.
- §5 데이터 계층 → Task 2(pagination) + Task 3(api).
- §6 기록 페이지 → Task 4(SegmentTabs) + Task 5(카드) + Task 6(Records).
- §7 기록 양식 → Task 7(폼 리팩터) + Task 8(래퍼).
- §8 설정 버전 표기 → Task 10 Step 4.
- §9 CSS → Task 4/6/9/10에 분산.
- §10 테스트 → Task 2·3(vitest), 각 UI task 수동 체크리스트.
- §11 마이그레이션 → Task 10(하드 컷오버, 스키마 무변경).

**타입 일관성:** `getRecordsPage({cursor,filter,limit})` / `getRecentRecords()` / `buildKeysetUnionQuery(cursor,limit)` / `nextUnionCursorFrom(rows,limit)` / `getRecordType(src,fuelTerm)` / 폼 ref `{submit,isDirty}` / `SegmentTabs` props — Task 정의와 소비처 일치 확인.

**미해결(스펙 §12, 구현 중 실측):** `.bottom-nav` 3칸 폭, 26px 아이콘 가독성 시 `PAD` 조정, 세그먼트 탭 sticky `top` 오프셋. 각 해당 task에 조정 지점 명시.
