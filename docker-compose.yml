version: "3.8"

services:
  carlog:
    build: .
    container_name: carlog
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DB_PATH=/data/carlog.db
    volumes:
      - ./data:/data
